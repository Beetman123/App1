<?xml version="1.0" encoding="utf-8"?>
<resources>
    <string name="CREATE_USER_SQL">
    CREATE TABLE IF NOT EXISTS User <!-- I dont think we use this (at least yet)-->
    (id TEXT PRIMARY KEY, shortDesc TEXT,
    longDesc TEXT, prereqs TEXT)
</string>

    <string name="DROP_USER_SQL">
    DROP TABLE IF EXISTS User
</string>


    <string name="CREATE_ITEM_SQL">
    CREATE TABLE IF NOT EXISTS Items
        (title TEXT, category TEXT, description TEXT, username TEXT,
        condition TEXT, price TEXT, trade TEXT, tradeFor TEXT, date TEXT)
    <!--(id TEXT PRIMARY KEY, shortDesc TEXT, &lt;!&ndash; Warning - !!! I havent changed these fields !!!&ndash;&gt;
    longDesc TEXT, prereqs TEXT)-->
</string>

    <string name="DROP_ITEM_SQL">
    DROP TABLE IF EXISTS Items
</string> <!-- should it stay 'Course' or be 'Item'-->


</resources>